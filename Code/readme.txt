Runninf code in Matlab:

Put input data in /Data/InputData/ directory.

Inputdata is in sparse aml file format (user, items) pairs, 
if we any have additional data like raitings (they are neglected).

Open CreateSyntheticDataset.m script and adjust parameters for MBRW model
or default ones.

Run CreateSyntheticDataset.m script

Synthetic dataset is saved to /Data/InputData/ directory
